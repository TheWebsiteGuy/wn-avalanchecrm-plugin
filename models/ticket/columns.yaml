# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    subject:
        label: Subject
        searchable: true
    client:
        label: Client
        relation: client
        select: name
        searchable: true
    project:
        label: Project
        relation: project
        select: name
        searchable: true
    category:
        label: Category
        relation: category
        select: name
        searchable: true
    status:
        label: Status
        searchable: true
    priority:
        label: Priority
        searchable: true
